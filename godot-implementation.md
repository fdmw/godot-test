# Godot 实现规范

- 适用版本：Godot 4.7.x
- 用途：约束 Godot 层面的长期实现方式，避免重复造轮子、隐式耦合、字符串反射、运行时状态混乱和离散逻辑被帧循环破坏。
- 本项目范围限定为 2D 和通用 Godot 功能，不实现或引入 3D 节点、3D 场景、3D 资源及 3D 专项验证。

本规范采用领域状态驱动的离散逻辑架构。有关权威状态、事务和逻辑时间的条款属于项目架构约束。明确采用连续物理模拟的系统，应在对应设计中定义状态所有权及同步边界。

## 原生碰撞与几何事实

具有持续空间身份、需要参与物理层筛选或接触查询的对象，优先使用 Godot 原生 `CollisionObject2D` 及其碰撞几何体系表达。

`PhysicsBody2D`、`Area2D` 等碰撞对象通过 `CollisionShape2D` 引用 `Shape2D`，或通过 `CollisionPolygon2D` 的多边形数据生成碰撞几何。上述几何定义是空间占用的唯一事实来源。

只在一次查询中使用的候选区域或瞬时几何，可以直接使用 `Shape2D` 和类型化空间查询参数，不为每次查询额外创建长期 Node。选择节点或直接查询时，以空间身份和生命周期为依据，不同时维护两套几何事实。

已经由碰撞几何表达的空间占用，不再额外维护一套重复的逻辑半径或包围盒作为第二事实来源。不表示实体占用的逻辑距离、范围和选择条件仍可进行纯几何计算。

领域运行时状态提供规则所需的权威位置及其他可变空间状态。Node Transform 用于将其映射到 Godot 物理空间，不得让动画插值位置反向覆盖领域状态。

空间查询统一组合领域权威 Transform 与碰撞几何局部 Transform，并纳入影响碰撞结果的启用状态。表现动画不得修改参与规则查询的碰撞 Transform 或几何；抖动、挤压等视觉变化由独立表现节点承担。

## 空间查询与事务同步

Node Transform 与 PhysicsServer 状态不保证在任意调用时点立即同步。领域逻辑不得假设刚修改 Transform 后的直接空间查询已经反映新位置。

同一逻辑事务中的空间查询属于验证过程。查询使用领域运行时状态或事务候选状态表达权威空间状态；全部验证成立后一次性提交领域状态，不得先修改领域状态或 Node Transform，再以空间查询失败为由回滚。

查询形状使用候选 Transform。`PhysicsShapeQueryParameters2D.transform` 只指定查询形状的 Transform，不替换物理空间中被查询对象的 Transform。

被查询动态实体的物理代理与当前查询状态不一致时，必须排除旧代理，并依据同一份碰撞几何及该实体的候选 Transform 完成补充检测。新增实体应纳入候选检测，已移除或已停用实体应排除其残留代理。

上述一致性要求覆盖所有尚未同步到物理空间的变化，包括当前事务的候选变化、前一事务已经提交的位置变化，以及实体新增、移除和碰撞启停。

直接空间查询统一在 `_physics_process()` 的安全调用上下文中执行。上下文外收到的请求先排队，已处于安全上下文的请求不额外等待下一帧。请求执行时，应确认其所属上下文仍然有效，并依据当前权威状态重新完成最终验证。

物理帧入口可以消费已经存在的请求，调用应用或领域接口，同步完成查询、验证和提交。领域行为的产生依据是请求，逻辑时间由领域所有者推进，与物理帧次数无关。原子领域事务不跨帧等待。

`Area2D.get_overlapping_*()` 等重叠查询读取按物理步更新的结果，不作为刚改变空间状态后的即时事务验证依据。

规则查询应明确碰撞 Mask、是否查询 Body / Area、排除对象及几何边界条件。需要完整候选集时，必须保证结果未被查询容量截断；达到容量上限时，应扩容重查或采用具有明确完整性保证的查询策略，再按规则需要完成领域实体去重和稳定排序。

## Collision Layer / Mask 统一管理

Collision Layer / Mask 使用 Project Settings 中具有明确语义的命名层统一管理。

业务代码通过集中定义的具名常量或明确接口使用层和掩码，不散落无法识别的魔法位值。集中定义应与 Project Settings 中的层语义保持一致。

具体层数量按实际需求增加，不提前建立无用层。

## 主动行为优先直接函数调用

具有明确调用目标的命令、请求和业务行为优先使用显式函数调用。

不使用全局事件、字符串调用或间接广播代替明确的业务接口。

## Signal 用于事件通知

Signal 主要表达：

> 某件事情已经发生。

行为发起优先函数调用，状态变化通知和松耦合观察优先 Signal。

代码连接 Signal 使用一等 Signal 和函数 / `Callable` 引用，不使用字符串 Signal 名或字符串函数名。

对外状态通知在领域事务完整提交后发出。Signal 默认连接会同步执行回调，通知回调不得重入尚未结束的事务；回调产生的新请求由统一入口按明确顺序处理。

动态绑定应避免重复连接，更换绑定或结束观察关系时应解除对应连接。

## 禁止字符串式代码分派

核心游戏逻辑不使用字符串方法名实现行为分派。

优先使用：

- 明确类型；
- 直接函数引用；
- `Callable`；
- enum；
- 明确对象接口。

固定业务契约优先使用明确类型和直接方法调用，`Callable` 本身不替代完整的静态类型接口。

稳定内容 ID、Tag 等数据字符串不属于该限制范围。引擎 API 必需的属性名等字符串由明确的引擎适配代码使用，不据此建立字符串式业务分派。

## 节点引用与外部依赖

场景管理自己的内部节点引用。

场景内部稳定节点可以使用类型化引用、Scene Unique Name 或稳定的局部路径。Scene Unique Name 只用于场景自身的封装边界，不作为跨场景依赖定位机制。

可复用场景不得依赖外部 SceneTree 的具体深层路径。

需要外部业务对象时，由拥有者显式提供依赖并保存为类型化引用，不主动遍历场景树寻找核心服务。只有确实面对动态结构时才进行受控动态查找。

编辑器中可配置且缺少必要子节点、Resource 或参数就无法正常工作的可复用场景，应按需要在 `@tool` 脚本中实现 `_get_configuration_warnings() -> PackedStringArray`。影响警告结果的编辑器配置发生变化时，应调用 `update_configuration_warnings()`。

编辑器配置警告用于提前反馈问题；必要运行时依赖仍应在初始化或统一验证入口检查。

依赖取得优先级：

```text
拥有者创建或显式注入的外部依赖
> 场景内部类型化稳定节点引用
> 受控动态查找
```

## Group 使用边界

Group 用于：

- 分类；
- 查询；
- 合理的批量操作。

Group 不作为核心系统的服务定位或依赖注入机制。

确定性领域流程不依赖 `get_first_node_in_group()` 或 `call_group()` 建立隐式业务关系。

## Autoload 使用边界

Autoload 只用于确实具有跨场景全局生命周期、且职责范围明确的少量基础服务。

由局部场景、流程或会话拥有的业务系统，不得仅为方便访问而设为 Autoload。

能由拥有者创建、当前场景持有或显式传递的依赖，不提升为全局对象。Autoload 不作为 Service Locator，也不持有本应属于局部上下文的可变业务状态。

无状态共享函数优先使用静态函数或普通轻量对象；共享定义数据优先使用只读 Resource。

## GDScript 核心接口类型化

核心 GDScript 代码使用一致的静态类型风格。公共接口、跨对象边界和影响玩法结果的代码必须静态类型化。

重点类型化：

- 公共函数参数；
- 返回值；
- Resource 字段；
- 运行时状态；
- Signal 参数；
- 领域请求与结果；
- 核心成员变量。

局部变量可在类型明确时使用类型推导。

引擎虚方法同样标注参数和返回值。能够准确表达元素、键和值类型时，使用类型化 `Array` 和 `Dictionary`；无法准确表达结构的动态容器只用于确有需要的数据边界，并在进入核心逻辑前完成验证和类型转换。

静态类型代码优先使用 `absi()`、`absf()`、`clampi()`、`clampf()`、`lerpf()` 等类型明确的全局函数。

## enum、ID 与 Tag 的职责边界

有限且封闭的程序状态使用 enum 或明确类型。

开放式内容身份和分类使用稳定字符串 ID / Tag。

不使用裸字符串替代封闭程序状态。

## 离散逻辑不由帧循环推进

影响权威状态的离散逻辑必须通过明确的应用或领域入口推进。

`_process()` 和 `_physics_process()` 不按帧自动产生领域命令、结算离散状态或推进逻辑时间。帧循环用于确实需要连续更新的表现、插值、渲染、输入采样和引擎交互。

物理帧入口可以按“空间查询与事务同步”一节消费已经存在的请求，并调用权威入口完成执行；不得自行产生领域行为。

不需要持续帧更新的 Node 不保持 `_process()`、`_physics_process()` 或输入处理回调活跃。临时需要连续更新时，通过对应的 `set_process*()` 方法按生命周期明确启停，不仅依赖回调内部长期执行 `if not active: return` 表达停用状态。

离散逻辑时间只由其明确的领域所有者推进。子系统、表现回调和 Scene 切换不得各自维护或推进平行时间线。

## 离散逻辑时间不使用 Timer 代替

属于离散领域规则的持续时间和调度统一使用领域定义的逻辑时间。具体推进顺序、计数边界和结算时点由对应设计事实决定，不在 Godot 实现规范中重复定义。

`Timer`、真实秒数和动画时长只用于表现或明确属于现实时间的功能。不得通过 `Timer` 或 `await create_timer()` 实现本应由离散逻辑时间推进的领域规则。

## 输入层只产生请求

输入和 UI 不直接修改真实游戏状态。

流程应为：

```text
输入 / UI
→ 类型化请求
→ 应用或领域入口
→ 合法性判断与候选计算
→ 提交权威状态
→ 事件通知
→ UI / 表现更新
```

领域运行时状态是权威状态来源。输入层可以预检，但最终合法性判断必须由应用或领域权威入口负责。

业务输入使用 InputMap 中具有稳定语义的 Action，并由输入层将其转换为类型化请求。除按键绑定、文本输入、设备诊断以及确实需要处理原始设备事件的底层代码外，不在游戏逻辑中硬编码键盘、鼠标或手柄物理按键。

业务代码只消费语义 Action 或类型化请求；快捷键显示从集中绑定事实或统一格式化入口取得，不在多个场景中分别复制物理键名；输入层不得把特定设备键值写入领域请求或领域运行时状态。

## 内容 Resource 运行时只读

用于内容定义的 Resource 在运行时视为共享、只读的数据源。

不得通过修改共享 Resource 表达某个实例的当前状态。

确实需要实例独立 Resource 时，应显式复制或采用适当的本地化机制，不依赖隐式共享行为。

不得笼统地把 `duplicate()` 视为完整深拷贝契约。复制 Resource 时必须明确嵌套 Resource、Array 和 Dictionary 的共享关系，并按目标版本的 `duplicate(deep)` 或 `duplicate_deep()` 语义选择复制策略。向外暴露可变容器时，应通过受控接口或明确副本避免调用方意外修改共享数据。

`resource_local_to_scene` 只用于确实需要每个场景实例独立副本的场景资源，不作为普通运行时状态模型。

具有独立生命周期或可变状态的运行实例，不得通过复制并修改内容 Resource 作为默认状态模型。固定定义继续由共享 Resource 提供，实例数据使用独立的运行时或持久状态对象保存。

## 纯逻辑对象不滥用 Node

只有需要 SceneTree 生命周期、Transform、物理、渲染、输入等 Node 能力的对象才使用 Node。

纯逻辑和短生命周期对象优先使用 `RefCounted` 等轻量运行时类型，例如领域请求、运行时状态、查询结果、值对象和纯计算器。需要序列化的静态内容优先使用 Resource。

`RefCounted` 对象避免形成无法解除的强引用环；双向关系通过明确解绑、稳定 ID 或适当的弱引用管理。

Scene 和业务对象优先通过组合、明确依赖和数据驱动形成差异，不建立仅用于逐层增加少量字段或转发行为的深层 Scene / Script 继承树。

共同且稳定、具有明确 `is-a` 关系的行为可以使用浅层继承；内容差异优先由 Resource 表达，能力和生命周期差异优先由具有独立职责的组合对象或子节点表达。

不为消除少量重复建立职责模糊的抽象基类，也不以组合为名增加没有独立状态、行为或生命周期价值的纯转发组件。

## GDScript 警告必须可验证

核心代码应消除适用的 `UNSAFE_*` 和 `UNTYPED_DECLARATION` 问题。只有在 Project Settings 中显式配置对应警告，并由统一验证入口检查时，才把警告等级视为项目级自动化约束；不得仅凭编辑器本地设置宣称已经启用。

核心代码不长期保留未解释的 unsafe 调用或弱类型行为。

确有必要忽略警告时，只针对明确位置局部处理，并保证原因清楚。

不要求将所有 Godot Warning 无条件提升为 Error。

## Node 初始化与生命周期

运行时创建 Node 时，能够提前确定的配置和依赖应尽量在加入 SceneTree 前完成。

进入 SceneTree 后再执行依赖 SceneTree、物理世界或生命周期的初始化。避免 `_ready()` 已执行后才补充对象正常运行所必需的核心依赖。

## 场景数据与脚本职责分离

静态场景数据应固定在 `.tscn` 或其引用的静态 Resource 中。节点层级、Control 布局、anchors、offsets、尺寸、初始位置、静态文本、默认属性、碰撞几何、导航几何以及基础资源引用，不应由 `.gd` 在 `_ready()` 中动态搭建。

`.gd` 负责场景节点引用、Signal 连接、用户操作、运行时状态修改和动态反馈。只有动态实例化、运行时生成内容等本身属于被验证目标时，才允许运行时创建 Node 或 Resource，并明确其生命周期；这不能替代静态场景布局。

仍在 SceneTree 中的 Node 默认使用 `queue_free()` 请求销毁。只有对象尚未加入或已经脱离 SceneTree、当前流程确实要求同步释放，并且已确认当前调用栈、引擎处理阶段、延迟回调和外部持有者均允许安全销毁时，才直接调用 `free()`。脱离 SceneTree 本身不构成同步释放安全的充分条件。

调用 `queue_free()` 后，对象立即视为待销毁状态，退出领域可交互集合，不得继续参与新的输入、领域调用或异步任务。物理代理尚未实际移除时，后续规则查询必须过滤其残留代理。

跨帧、异步或延迟持有 Node 引用时，先使用 `is_instance_valid()` 判断实例是否仍然存在；确认有效后，再按需要检查 `is_queued_for_deletion()`、`is_inside_tree()`，以及对象是否仍属于当前绑定或任务代次。

`await` 返回后不得假设发起者、目标 Node、SceneTree 或绑定上下文仍然有效。短生命周期异步任务应具有明确的取消、代次失效或身份复核机制，并在退出场景树或更换绑定时清理外部引用。

异步只用于明确允许跨帧的流程和必要的引擎帧交互。`call_deferred()` 不作为普通业务调用方式，也不用于掩盖未明确的初始化或结算时序。

引擎要求延迟执行的操作应遵守对应 API 约束，例如通过 `set_deferred()` 修改碰撞几何节点的 `disabled`。领域状态按事务时序提交，延迟的引擎代理更新遵守“空间查询与事务同步”一节的一致性要求。

## 影响结果的集合必须稳定排序

凡是处理顺序可能影响游戏逻辑结果的集合，都必须使用明确且稳定的排序依据。

不得依赖：

- SceneTree 当前子节点顺序；
- Group 查询返回顺序；
- 文件系统遍历顺序；
- 资源扫描天然顺序；
- 物理查询返回顺序；
- 其他未明确保证稳定性的引擎枚举顺序。

排序依据应包含明确的业务优先级，并在优先级相同时继续使用稳定唯一的领域身份完成决胜。不得默认以 ObjectID、RID 或引擎实例创建顺序作为可复现的领域身份。

影响结果的排序比较必须形成完整且唯一的顺序，不得依赖 `Array.sort()` 或 `sort_custom()` 保持比较结果相等元素的原有顺序。

顺序影响结果的流程采用明确且可复现的顺序。具体稳定身份、业务优先级和生命周期由对应设计事实定义，本规范只约束实现不得依赖 Godot 容器或引擎查询的偶然顺序。

稳定排序约束处理顺序，不单独构成跨平台物理结果完全一致的保证。
